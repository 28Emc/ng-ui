import { Meta, Canvas, Controls, Source } from '@storybook/addon-docs/blocks';
import * as RadioStories from './radio.stories';

<Meta of={RadioStories} />

# Radio

Opción individual de un grupo de radio. Se usa siempre dentro de `ui-radio-group`, que le
proporciona el contexto de selección y teclado (`role="radiogroup"`).

<Canvas of={RadioStories.WithGroup} />
<Controls of={RadioStories.WithGroup} />

## Compacto

<Canvas of={RadioStories.Compact} />

## Uso

<Source
  code={`<ui-radio-group label="Plan">
  <ui-radio value="free" label="Gratuito" description="0 €/mes" />
  <ui-radio value="pro" label="Profesional" description="12 €/mes" />
</ui-radio-group>`}
  language="html"
/>
